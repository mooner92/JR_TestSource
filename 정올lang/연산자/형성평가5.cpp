#include <stdio.h>
int main()
{
    int m_height, m_weight, k_height, k_weight;
    scanf("%d %d %d %d", &m_height, &m_weight, &k_height, &k_weight);
    if (m_height > k_height && m_weight > k_weight)
        printf("0");
    else
        printf("0");
}